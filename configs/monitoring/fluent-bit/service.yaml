apiVersion: v1
kind: Service
metadata:
  name: fluent-bit
  namespace: fitness-platform-production
  labels:
    app: fluent-bit
spec:
  type: ClusterIP
  selector:
    app: fluent-bit
  ports:
    - name: metrics
      port: 2020
      targetPort: 2020
