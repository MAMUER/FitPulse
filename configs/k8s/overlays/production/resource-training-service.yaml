apiVersion: apps/v1
kind: Deployment
metadata:
  name: training-service
  namespace: fitness-platform-production
spec:
  template:
    spec:
      containers:
        - name: training-service
          resources:
            requests:
              cpu: 25m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
