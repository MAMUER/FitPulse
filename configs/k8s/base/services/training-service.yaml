apiVersion: v1
kind: Service
metadata:
  name: training-service
  namespace: fitness-platform-production
spec:
  selector:
    app: training-service
  ports:
    - port: 50053
      targetPort: 50053
    - port: 9095
      targetPort: 9095
      name: metrics
  type: ClusterIP
