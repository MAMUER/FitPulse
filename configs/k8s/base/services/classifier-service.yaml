apiVersion: v1
kind: Service
metadata:
  name: classifier
  namespace: fitness-platform-production
spec:
  selector:
    app: classifier
  ports:
    - port: 8001
      targetPort: 8001
    - port: 9091
      targetPort: 9091
      name: metrics
  type: ClusterIP
