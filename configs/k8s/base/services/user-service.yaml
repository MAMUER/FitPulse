apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: fitness-platform-production
spec:
  selector:
    app: user-service
  ports:
    - port: 50051
      targetPort: 50051
    - port: 9096
      targetPort: 9096
      name: metrics
  type: ClusterIP
