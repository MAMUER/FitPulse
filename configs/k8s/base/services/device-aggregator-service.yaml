apiVersion: v1
kind: Service
metadata:
  name: device-aggregator-service
  namespace: fitness-platform-production
  labels:
    app: device-aggregator
    tier: backend
    component: device-aggregator
spec:
  type: ClusterIP
  selector:
    app: device-aggregator
  ports:
    - port: 8083
      targetPort: 8083
      name: http
