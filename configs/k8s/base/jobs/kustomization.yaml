apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

configMapGenerator:
  - name: init-db-sql
    files:
      - init.sql=init-db.sql    # ← Теперь путь относительно этой директории

resources:
  - migrate-db.yaml