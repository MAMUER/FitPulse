apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: external-secrets
  labels:
    app: external-secrets

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: external-secrets-role
  labels:
    app: external-secrets
  rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list", "watch"]
  - apiGroups: ["external-secrets.io"]
    resources: ["secretstores", "clustersecretstores", "externalsecrets", "pushsecrets"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]

---
# checkov:skip=CKV_K8S_20:External Secrets Operator requires namespace-scoped secret write access
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: external-secrets-writer
  namespace: fitness-platform-production
  labels:
    app: external-secrets
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["get", "list", "watch", "create", "update", "patch"]

---
# checkov:skip=CKV_K8S_20:External Secrets Operator needs namespace-scoped secret write access
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: external-secrets-writer-binding
  namespace: fitness-platform-production
  labels:
    app: external-secrets
  annotations:
    checkov:skip: CKV_K8S_20: ESO requires write access to secrets in target namespace
metadata:
  name: external-secrets-writer-binding
  namespace: fitness-platform-production
  labels:
    app: external-secrets
subjects:
  - kind: ServiceAccount
    name: external-secrets-sa
    namespace: external-secrets
roleRef:
  kind: Role
  name: external-secrets-writer
  apiGroup: rbac.authorization.k8s.io
