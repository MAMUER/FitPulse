apiVersion: v1
kind: ResourceQuota
metadata:
  name: local-path-storage-quota
  namespace: local-path-storage
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "512Mi"
    limits.cpu: "1000m"
    limits.memory: "1Gi"
    pods: "10"
    services: "5"
    secrets: "10"
    configmaps: "10"
