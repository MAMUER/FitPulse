apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-service
  namespace: fitness-platform-production
  labels:
    app: user-service
    tier: backend
spec:
  selector:
    matchLabels:
      app: user-service
  namespaceSelector:
    matchNames:
      - fitness-platform-production
  endpoints:
    - port: metrics
      path: /metrics
      interval: 30s
      scrapeTimeout: 10s
