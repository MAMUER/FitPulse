apiVersion: v1
kind: Service
metadata:
  name: gateway-canary-service
  namespace: fitness-platform
  labels:
    app: gateway-canary
    tier: frontend
spec:
  selector:
    app: gateway-canary
  ports:
  - port: 8080
    targetPort: 8080
    name: http
  type: ClusterIP
